# AGENTS.md

Rule: You must always read and comply with all `.agent/` rules and workflows. Apply any rule/workflow with Activation Mode `always_on` automatically. Apply Activation Mode `manual` only when explicitly invoked by the user or when the task clearly requests that rule/workflow.
